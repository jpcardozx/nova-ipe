// DEPRECATED: Este arquivo está sendo substituído por instrumentation.ts
// Mantendo arquivo mínimo para compatibilidade durante a transição
// https://nextjs.org/docs/app/building-your-application/optimizing/instrumentation

import * as Sentry from "@sentry/nextjs";

// Export required hooks for backwards compatibility
export const onRequestError = Sentry.captureRequestError;

// Não inicializar aqui - a inicialização acontece no instrumentation.ts
