/**
 * Módulo stub criado automaticamente para @/types/sanity-schema
 * Este módulo é uma implementação minimalista para permitir o build
 */
export default function Sanity-schema(props) {
  return {
    ...props,
    _isStub: true
  };
}
