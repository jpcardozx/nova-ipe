'use client';

/**
 * A simplified component that safely replaces the ClientPerformanceMonitor
 * to avoid runtime errors.
 */
export default function ClientPerformanceMonitor() {
    // Return null to prevent any rendering issues
    return null;
}
